spring:
  application:
    name: proxy-service
  cloud:
    config:
      uri: http://localhost:8888

zuul:
  host:
    socket-timeout-millis: 60000
  add-host-header: true
  add-proxy-headers: true
  ignored-services: "*"
